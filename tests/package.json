{
  "name": "noteme-tests",
  "private": true,
  "type": "module",
  "description": "Tes logika & UI NoteMe. Sengaja punya package.json sendiri agar dependensi tes tidak menyentuh lockfile proyek utama (bun.lock dikelola Lovable).",
  "scripts": {
    "test": "tsx --tsconfig ./tsconfig.json --test --test-concurrency=1 \"./**/*.test.mts\""
  },
  "devDependencies": {
    "jsdom": "^30.1.0",
    "tsx": "^4.23.13"
  }
}
